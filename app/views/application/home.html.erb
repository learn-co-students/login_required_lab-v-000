<% if logged_in? %>

  <h1>HI, <%= current_user %>!</h1>
  
  <%= form_tag logout_path do %>
    <%= submit_tag 'Logout' %>
  <% end %>
  
<% else %>

  <%= link_to 'Login', login_path %>
  
<% end %>

